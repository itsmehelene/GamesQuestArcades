<h1><%= @arcade.name %></h1>

<div>
  <h3><% @user.reservation.each do |reservation| %></h3>
  <img src="https://images.unsplash.com/photo-1520278013317-50b1dfd757cd?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=60" alt="">
  <p><%= reservation.day %></p>
  <%= link_to 'Delete an reservation', user_reservation_path(reservation), :method => :delete %>
  <% end %>
</div>


<%= link_to 'Add an reservation',new_user_reservation_path(@reservation) %>
<br>
<%= link_to 'Back', user_reservations_path %>
